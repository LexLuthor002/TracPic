## Packages
framer-motion | For page transitions and scroll-triggered animations
date-fns | For human-readable date formatting in the chat interface
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes without style conflicts

## Notes
Static images: import logo from @assets/images_-_2026-02-24T174110.730_1771957584430.jpeg
Backend might not be fully implemented, so API hooks should fallback gracefully to realistic mock data.
Websocket/Intercom chat layer relies on mocked P2P state if endpoints fail.
